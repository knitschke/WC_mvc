@model IEnumerable<WC_mvc.Models.WorldCup>

@{
    ViewBag.Title = "Index";
}


<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Location)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Year)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Location)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Year)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.WC_Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.WC_Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.WC_Id })
        </td>
    </tr>
}

</table>
