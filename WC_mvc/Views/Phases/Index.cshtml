@model IEnumerable<WC_mvc.Models.Phase>

@{
    ViewBag.Title = "Index";
}

<h3>
    @Html.ActionLink("Create New", "Create")
</h3>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PhaseName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.WorldCup.Location)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PhaseName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.WorldCup.Location)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Phase_Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Phase_Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Phase_Id })
        </td>
    </tr>
}

</table>
